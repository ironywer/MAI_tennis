# MAI_tennis

Для запуска сервера введите в командную строку:
```bash
go run ./cmd/server
```

После запуска сервера у вас появится возможность проверить поднялся ли он через команду:
```bash
curl.exe -i http://localhost:8080
```

И зайти на главную страницу по адресу http://localhost:8080